<Window x:Class="MetalBayalaGestion.Views.UserEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Utilisateur" Height="420" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        FontFamily="{DynamicResource MaterialDesignFont}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="Utilisateur" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Nom d'utilisateur :" VerticalAlignment="Center" Margin="0,5"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding User.Username}" md:HintAssist.Hint="Nom d'utilisateur" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,5"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Nom complet :" VerticalAlignment="Center" Margin="0,5"/>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding User.FullName}" md:HintAssist.Hint="Nom complet" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,5"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Rôle :" VerticalAlignment="Center" Margin="0,5"/>
        <ComboBox Grid.Row="3" Grid.Column="1" ItemsSource="{Binding Roles}" SelectedItem="{Binding User.Role}" Style="{StaticResource MaterialDesignOutlinedComboBox}" Margin="0,5"/>

        <TextBlock Grid.Row="4" Grid.Column="0" Text="Mot de passe :" VerticalAlignment="Center" Margin="0,5"/>
        <PasswordBox Grid.Row="4" Grid.Column="1" x:Name="PasswordBox" md:HintAssist.Hint="Laisser vide pour conserver" Style="{StaticResource MaterialDesignOutlinedPasswordBox}" Margin="0,5" PasswordChanged="PasswordBox_PasswordChanged"/>

        <TextBlock Grid.Row="5" Grid.Column="0" Text="Confirmation :" VerticalAlignment="Center" Margin="0,5"/>
        <PasswordBox Grid.Row="5" Grid.Column="1" x:Name="ConfirmBox" md:HintAssist.Hint="Confirmer le mot de passe" Style="{StaticResource MaterialDesignOutlinedPasswordBox}" Margin="0,5" PasswordChanged="ConfirmBox_PasswordChanged"/>

        <CheckBox Grid.Row="6" Grid.Column="1" Content="Actif" IsChecked="{Binding User.IsActive}" Margin="0,10"/>

        <StackPanel Grid.Row="8" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Enregistrer" Command="{Binding SaveCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" Style="{StaticResource MaterialDesignRaisedButton}" Margin="0,0,10,0"/>
            <Button Content="Annuler" Command="{Binding CancelCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" Style="{StaticResource MaterialDesignOutlinedButton}"/>
        </StackPanel>
    </Grid>
</Window>
