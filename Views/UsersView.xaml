<UserControl x:Class="MetalBayalaGestion.Views.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Utilisateurs" FontSize="24" FontWeight="Bold" Margin="0,0,0,15"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Width="250" md:HintAssist.Hint="Rechercher..." Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,0,10,0"/>
            <Button Content="Ajouter" Command="{Binding AddUserCommand}" Style="{StaticResource MaterialDesignRaisedButton}" Margin="0,0,5,0"/>
            <Button Content="Modifier" Command="{Binding EditUserCommand}" Style="{StaticResource MaterialDesignRaisedButton}" Margin="0,0,5,0"/>
            <Button Content="Supprimer" Command="{Binding DeleteUserCommand}" Style="{StaticResource MaterialDesignRaisedButton}" Margin="0,0,5,0"/>
            <Button Content="Actualiser" Command="{Binding RefreshCommand}" Style="{StaticResource MaterialDesignOutlinedButton}"/>
        </StackPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Users}" SelectedItem="{Binding SelectedUser}"
                  AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"
                  Style="{StaticResource MaterialDesignDataGrid}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nom d'utilisateur" Binding="{Binding Username}" Width="150"/>
                <DataGridTextColumn Header="Nom complet" Binding="{Binding FullName}" Width="200"/>
                <DataGridTextColumn Header="Rôle" Binding="{Binding Role}" Width="150"/>
                <DataGridCheckBoxColumn Header="Actif" Binding="{Binding IsActive}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
