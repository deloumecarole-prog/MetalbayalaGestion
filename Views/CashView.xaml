<UserControl x:Class="MetalBayalaGestion.Views.CashView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Caisse" FontSize="24" FontWeight="Bold" Margin="8"/>

        <UniformGrid Grid.Row="1" Columns="3" Margin="8">
            <materialDesign:Card Padding="16" Margin="4">
                <StackPanel>
                    <TextBlock Text="Solde actuel" Opacity="0.6"/>
                    <TextBlock Text="{Binding CurrentBalance, StringFormat='{}{0:N0} FCFA'}" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                </StackPanel>
            </materialDesign:Card>
            <materialDesign:Card Padding="16" Margin="4">
                <StackPanel>
                    <TextBlock Text="Entrées" Opacity="0.6"/>
                    <TextBlock Text="{Binding TotalIn, StringFormat='{}{0:N0} FCFA'}" FontSize="20" FontWeight="Bold" Foreground="Green"/>
                </StackPanel>
            </materialDesign:Card>
            <materialDesign:Card Padding="16" Margin="4">
                <StackPanel>
                    <TextBlock Text="Sorties" Opacity="0.6"/>
                    <TextBlock Text="{Binding TotalOut, StringFormat='{}{0:N0} FCFA'}" FontSize="20" FontWeight="Bold" Foreground="Red"/>
                </StackPanel>
            </materialDesign:Card>
        </UniformGrid>

        <ToolBarTray Grid.Row="2" Margin="8,0">
            <ToolBar>
                <Button Command="{Binding AddTransactionCommand}" ToolTip="Ajouter">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Plus" Margin="0,0,4,0"/>
                        <TextBlock Text="Ajouter"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding RefreshCommand}" ToolTip="Actualiser">
                    <materialDesign:PackIcon Kind="Refresh"/>
                </Button>
            </ToolBar>
        </ToolBarTray>

        <DataGrid Grid.Row="3" Margin="8" ItemsSource="{Binding Transactions}" 
                  AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat='{}{0:dd/MM/yyyy}'}" Width="Auto"/>
                <DataGridTextColumn Header="Libellé" Binding="{Binding Label}" Width="*"/>
                <DataGridTextColumn Header="Catégorie" Binding="{Binding Category}" Width="Auto"/>
                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="Auto"/>
                <DataGridTextColumn Header="Montant" Binding="{Binding Amount, StringFormat='{}{0:N0}'}" Width="Auto"/>
                <DataGridTextColumn Header="Mode" Binding="{Binding Mode}" Width="Auto"/>
                <DataGridTextColumn Header="Référence" Binding="{Binding Reference}" Width="Auto"/>
                <DataGridTemplateColumn Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Command="{Binding DataContext.DeleteTransactionCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                    CommandParameter="{Binding}" Style="{StaticResource MaterialDesignFlatButton}">
                                <materialDesign:PackIcon Kind="Delete"/>
                            </Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>