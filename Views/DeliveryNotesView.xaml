<UserControl x:Class="MetalBayalaGestion.Views.DeliveryNotesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Bons de sortie" FontSize="24" FontWeight="Bold" Margin="0,0,0,15"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Width="250" md:HintAssist.Hint="Rechercher..." Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,0,10,0"/>
            <Button Content="Ajouter" Command="{Binding AddDeliveryNoteCommand}" Style="{StaticResource MaterialDesignRaisedButton}" Margin="0,0,5,0"/>
            <Button Content="Modifier" Command="{Binding EditDeliveryNoteCommand}" Style="{StaticResource MaterialDesignRaisedButton}" Margin="0,0,5,0"/>
            <Button Content="Supprimer" Command="{Binding DeleteDeliveryNoteCommand}" Style="{StaticResource MaterialDesignRaisedButton}" Margin="0,0,5,0"/>
            <Button Content="Exporter PDF" Command="{Binding PrintDeliveryNoteCommand}" Style="{StaticResource MaterialDesignOutlinedButton}" Margin="0,0,5,0"/>
            <Button Content="Actualiser" Command="{Binding RefreshCommand}" Style="{StaticResource MaterialDesignOutlinedButton}"/>
        </StackPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding DeliveryNotes}" SelectedItem="{Binding SelectedDeliveryNote}"
                  AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"
                  Style="{StaticResource MaterialDesignDataGrid}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Numéro" Binding="{Binding Number}" Width="130"/>
                <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat=dd/MM/yyyy}" Width="100"/>
                <DataGridTextColumn Header="Client" Binding="{Binding Client.Name}" Width="250"/>
                <DataGridTextColumn Header="Adresse livraison" Binding="{Binding DeliveryAddress}" Width="200"/>
                <DataGridTextColumn Header="Statut" Binding="{Binding Status}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
